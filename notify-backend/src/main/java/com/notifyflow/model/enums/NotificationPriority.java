package com.notifyflow.model.enums;

/**
 * Priority levels for notifications.
 * LOW priority notifications are suppressed during quiet hours.
 */
public enum NotificationPriority {
    HIGH,
    NORMAL,
    LOW
}